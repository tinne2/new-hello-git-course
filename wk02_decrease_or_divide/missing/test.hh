#ifndef MISSING_HH
#define MISSING_HH

const int NO_VALUE_MISSING = -1;

//int searchSmallestMissing(int* arr);
int searchSmallestMissingIteration(int* arr, int size);
int searchSmallestMissing(int* arr, int left, int right);

#endif
